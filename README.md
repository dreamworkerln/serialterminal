# Node observations

This orphan branch stores run-specific LoRa-Chatter hardware evidence.

- Append new records under `observations/`.
- Do not merge this branch into `dev`.
- Local executor agents must not modify `REVIEW_STATE.md`.
- Recording rules live on `dev` in `NODE_OBSERVATION_RECORDING_POLICY.md`.
- Reviewer processing rules live on `dev` in `NODE_SKILL_LEARNING_POLICY.md`.
