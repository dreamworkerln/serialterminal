# Node observation

Observed: 2026-09-18T00:25:08Z
Task: diagnostic concurrent BLE `/help` burst on two Chatter nodes
Result: PASS
SerialTerminal: dreamworkerln/serialterminal@4f562062afd64750679a4d5562a29a022025a967
Firmware: dreamworkerln/lora-sack-protocol@unknown

## Setup

- One long-lived SerialTerminal agent process; explicit `profile:"chatter"` for both BLE sessions.
- Targets: `LoRa-Chatter-1B44` and `LoRa-Chatter-72E0`.
- No flash, reboot, or power-cycle.

## Actions

- Confirmed both identities and `/chat` state.
- Executed three concurrent rounds in the requested order, with round 3 sent as one batch.

## Evidence

- All six `/help` responses were COMPLETE: three per node.
- All six sends reached `tx_state:"written"`; no send-outcome-unknown.
- No forensic gap, cursor expiry, or disconnect/reconnect during the burst.
- Same-node command overlap was not observed.

## Anomalies / conflicts

- none affecting this run
- No causal conclusion about data loss or corruption is made.

## Final state

- Both sessions reported `connected` with `queued_tx:0` before close, then closed successfully through the agent API.

Run bundle: runs/RUN_20260918T002508Z_ble-burst/
