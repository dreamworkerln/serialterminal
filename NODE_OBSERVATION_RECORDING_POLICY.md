# Hardware evidence policy moved

Canonical hardware executor evidence policy now lives on branch node_observations
in the independent sibling clone:

```text
serialterminal-observations/NODE_OBSERVATION_RECORDING_POLICY.md
```

Do not maintain a second copy on dev.

This dev-side file is only a migration pointer for source-development readers and
historical links.
